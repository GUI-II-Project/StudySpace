specVersion: "v1beta"
serviceId: "studyspace"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "studyspace-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
